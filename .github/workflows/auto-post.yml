name: 🤖 Tistory Auto-Poster

on:
  workflow_dispatch:
    inputs:
      topic:
        description: "포스팅 주제 (예: LangChain FAISS 벡터 검색 구현)"
        required: true
        type: string
      category_id:
        description: "카테고리 ID (기본: 0 = 미분류)"
        required: false
        default: "0"
        type: string

  schedule:
    - cron: "0 0 * * 1,3,5"

env:
  ANTHROPIC_API_KEY:   ${{ secrets.ANTHROPIC_API_KEY }}
  KAKAO_EMAIL:         ${{ secrets.KAKAO_EMAIL }}
  KAKAO_PASSWORD:      ${{ secrets.KAKAO_PASSWORD }}
  TISTORY_BLOG:        ${{ secrets.TISTORY_BLOG }}
  DISCORD_WEBHOOK:     ${{ secrets.DISCORD_WEBHOOK }}
  POST_TOPIC:          ${{ github.event.inputs.topic || vars.DEFAULT_TOPIC }}
  TISTORY_CATEGORY_ID: ${{ github.event.inputs.category_id || '0' }}

jobs:
  auto-post:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 🐍 Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: 📦 Install dependencies
        run: |
          pip install -r requirements.txt
          playwright install chromium
          playwright install-deps chromium

      - name: 🚀 Generate & Publish Post
        run: python run.py

      - name: 💾 Upload artifacts (항상 저장)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: post-debug-${{ github.run_number }}
          path: |
            post_output.json
            editor_snapshot.html
            error_screenshot.png
            error_page.html
            success_screenshot.png
          retention-days: 7

      - name: ❌ Notify failure
        if: failure()
        run: |
          curl -s -X POST "$DISCORD_WEBHOOK" \
            -H "Content-Type: application/json" \
            -d "{\"content\": \"❌ **티스토리 자동 포스팅 실패** (Run #${{ github.run_number }})\n주제: $POST_TOPIC\n로그: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\"}" || true
